import {server} from '../package.json';

const { root } = server;

export const REQUESTS_URL = `${root}/requests/`;
export const PRODUCTS_URL = `${root}/products/`;
export const PRICING_URL = `${root}/pricing/`;
export const CLIENT_ORDERS_URL = `${root}/client_orders/`;
export const TRANSPORT_ORDERS_URL = `${root}/transport_orders/`;
